---
title: Infractions
description: Quickly and powerfully take action on staff violations
---

The **infractions** module allows you to easily track violations by your staff, and take automated actions accordingly.

<Accordion title="Infraction Types">
    | Type | Action |
    | ------ | ----------- |
    | Add Role | Adds a Discord role to the infracted user. |
    | Remove Role | Removes a Discord role from the infracted user. |
    | Channel | Sends the infraction log to a different channel. |
    | Removal From Staff Database | Removes the user from the **Staff Database** module. |
    | Remove From Group | Removes the user from a Roblox group. *(Requires Roblox Integration)* |
    | Change Group Role | Changes the user's Roblox group role. *(Requires Roblox Integration)* |
</Accordion>

## Configuration Options

<ParamField path="channel" type="channel" required>
    The channel to send infraction notifications to
</ParamField>
<ParamField path="audit log" type="channel" required>
    The channel to send infraction logs to
</ParamField>
<ParamField path="types" type="infraction" required>
    <ParamField path="name" type="string" required>
        The name of the infraction type
    </ParamField>
    <ParamField path="actions" type="action[]" required>
        The actions to take for this infraction
    </ParamField>
</ParamField>
<ParamField path="reasons" type="string[]">
    Predefined reasons for an infraction
</ParamField>

## Commands
<AccordionGroup>
    <Accordion title="/infraction issue">
        Issue an infraction against one (or more) of your members of staff.
        #### Parameters
        <ParamField path="staff" type="user" required>
            The staff member you are infracting
        </ParamField>
        <ParamField path="action" type="type" required>
            The type of infraction to issue
        </ParamField>
        <ParamField path="reason" type="string" required>
            The reason for infracting them
        </ParamField>
        <ParamField path="notes" type="string">
            Any additional notes
        </ParamField>
        <ParamField path="expiry" type="time">
            When this infraction expires
        </ParamField>
        <ParamField path="anonymous" type="boolean">
            Whether to send this infraction anonymously
        </ParamField>
    </Accordion>
    <Accordion title="/infraction list">
        List infractions from the server
        #### Parameters
        <ParamField path="staff" type="user">
            The staff member to view infractions for (default all)
        </ParamField>
        <ParamField path="scope" type="voided/expired/all">
            The scope of infractions to view (default all)
        </ParamField>
    </Accordion>
    <Accordion title="/infraction view">
        View a specific infraction by ID
        #### Parameters
        <ParamField path="id" type="number">
            Which infraction ID to view
        </ParamField>
        <ParamField path="voided" type="boolean">
            Whether to view this infraction even if it has been voided
        </ParamField>
    </Accordion>
    <Accordion title="/infraction multiple">
        (Premium only) Infract multiple staff members
        #### Parameters
        <ParamField path="action" type="type" required>
            The type of infraction to issue
        </ParamField>
        <ParamField path="reason" type="string" required>
            The reason for infracting them
        </ParamField>
        <ParamField path="notes" type="string">
            Any additional notes
        </ParamField>
        <ParamField path="expiry" type="time">
            When this infraction expires
        </ParamField>
        <ParamField path="anonymous" type="boolean">
            Whether to send this infraction anonymously
        </ParamField>
    </Accordion>
</AccordionGroup>